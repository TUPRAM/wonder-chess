# Compact HUD source checkpoint and actual recovery review

Only `WCMatchHUD.cpp` was changed in this source checkpoint. It is frozen for root-owned compilation and capture. `git diff --check` passed, with the existing LF/CRLF notice; this document does not assert the new layout has executed.

Neutral previews retain actual model-derived portraits, creature counts, wave-scaled HP and attack, range and behavior. Cards now cap at 382 logical pixels wide and 108 high, instead of stretching one species across roughly 1,200 pixels and 166 high. At 720p this changes a one-species card from approximately 800 x 111 to 255 x 72 screen pixels. Four distinct species fit in the existing central strip; additional species wrap to another row. Placement hitboxes and commands are untouched.

The default in-match skill panel now wraps selected-star canonical ordered effects, appropriate target descriptions, meaningful nonzero duration/range/area and first-cast/cooldown values. It omits unused zero shield/radius/travel entries and irrelevant target limits for self/single-target/dash actions. Composite effects retain their order; Neris's stun is conditional on the target surviving. Damage/support bonuses use the existing runtime calculations. The full authored tooltips and advanced timing in the gallery are unchanged. Actual 720p/1080p capture is required for the updated cards and long descriptions before visual acceptance.

## Actual prior-DLL network recovery

Read-only inspection of `reports/WC-U430/20260906T161534Z/network-recovery-ui` found seven passing checks and zero failed checks/captures. Host and client exited 0 and bind the same DLL SHA256 `726fbefbcedafdace72c1006e4a60997432c6727fb81325b86925bd6ce031339`. The client log records an actual 20-second timeout at `127.0.0.1:17987`, followed by an actual server welcome at the declared retry endpoint `127.0.0.1:17986`, assigned seat 1, protocol 5 and matching catalog digest. The actual Slate Leave action returned to the local title with no running tournament. This is a software-driven two-process loopback recovery fixture, not physical LAN or human play.

All three screenshots were opened. Their capture timing prevents visual acceptance: the error/connected screens still have actual editor asset-preparation overlays and apparently first-layout paragraphs clipped at the panel edge. The returned title still shows a black sky and a sky-material warning. The audit calls screenshot capture immediately after state rebuild/travel, then waits four frames before the next operation. Root was informed that a future audit correction should wait for settled layout/readiness before capture and preserve the original actual callback/check evidence. These retained screenshots must not be relabeled visually clean.
