# First real UI observation: incomplete discovery run

The actual export at `reports/WC-350/first-ui` was parsed successfully. It is a single standalone Unreal process, PID 41292, with one manual Start1H7B action. There is no second network process and no completed tournament. This observation must not be promoted to 1H7B whole-match, command, restart, networking or busy-load acceptance.

The match export contains 123 received snapshots, all from round 1, covering preparation and combat. They consistently show one human and seven bots. Every received owner-private state belongs to seat 0; an independent recursive scan found none of the forbidden economy/shop/bench/sequence/RNG field names in the public snapshots. This is a received-state inspection in a standalone process, not a network packet privacy test. There were no actual gameplay command replies. `complete` and `aborted` are both false at bounded exit.

The observed viewport was 1920×1080 using D3D11 on **NVIDIA GeForce RTX 3060 Laptop GPU**, with **AMD Ryzen 7 6800H with Radeon Graphics** as CPU. The separately reported OS primary GPU was AMD Radeon graphics; the timing report correctly records the active RHI adapter. Across 7,650 profiled frames, frame interval p50/p95/p99 was **7.1407/8.9603/10.0617 ms**; GPU p50/p95/p99 was **7.1905/8.3692/9.3388 ms**. The maximum frame interval was 167.3554 ms, with three frames above 100 ms. These measurements include instrumentation and this discovery scene only. The actual render settings are preserved in `first-ui-frame-summary.json`; the first recorder revision did not yet write a simulation-speed field.

The export observed at most six visible living units, 28 logical living units, and three simultaneous live encounters. There were **zero samples with twelve visible living units**, therefore the required twelve-visible/four-live load condition is **NOT_RUN**. The measured discovery scene is not a substitute for that condition.

The real log contains **27 error lines** across nine animation loads. They report a missing target `USkeleton`, failed FKControlRig initialization and an invalid skeleton. Installed Unreal source confirms the first message is emitted when the owning animation's `GetSkeleton()` returns null. Root subsequently identified that generated skeleton assets were not explicitly saved: mesh/animation import task success had not persisted the generated skeletons for a cold load. Root is correcting importer persistence and reimporting. These errors remain preserved in the original log and `first-ui-observation.json`; an in-memory reimport or new test source alone does not clear them.

The actual round-1 combat screenshot was visually inspected. It shows the arena, four units, portraits and HUD, with foreground architecture occluding the lower board and low-contrast unit surfaces at this camera distance. Root is iterating the camera, materials and UI. This single still cannot verify any of the seven animation clips or complete hero art acceptance.

Full tournament, command probes, restart, 2H6B, disconnect/rejoin, 84 clip review and required busy-load measurement remain untested in this observation. Input paths, hashes and every error line are recorded in `first-ui-observation.json`; the separate frame summary preserves measurements without inferred FPS or hardware.
