# Execute Wonder Chess: playable-first alpha

Work in this new Wonder Chess workspace. Read `AGENTS.md`, `START_HERE.md`, `docs/MASTER_IMPLEMENTATION_v3.md`, and the task relevant to the next unblocked step.

The required end product is a **packaged Windows Unreal game with one human and seven persistent bots**, twelve attractive animated heroes, one coherent high-fantasy arena, a real eight-seat tournament, shop/bench/placement/upgrades/leveling, race/class synergies, scouting, elimination, spectating, results, audio, and restart. The same rules must support 0-human/8-bot regression and an actual 2-human/6-bot local-network check.

Do not clone, migrate, read-copy or modify VEILMARK. Do not build a website, browser game or data-only demo instead of the Unreal deliverable. The active contract is v3 in this kit; previous prompt versions and proposed discussions do not override it. Preserve genuine Wonder Chess progress when resuming.

Inspect real tool availability and versions first. Run the supplied baseline validators. Establish a new C++ Unreal project and package a minimal executable early. Then build the eight-seat loop with internal graybox pieces, complete Ada Brightshield and a calibrated board module in Blender/Unreal, integrate the remaining eleven alpha heroes, finish UI/audio and bot decisions, and verify complete packaged matches. Use the authored data and character dossiers instead of reinventing or silently abbreviating them.

Work through internal implement → execute → inspect → fix checkpoints. Passing a checkpoint is not an instruction to stop after documentation or bootstrap when independent implementation remains possible. Use task-scoped file ownership, logs and actual screenshots. Do not let concurrent workers edit the same binary, canonical schema or balance file.

The Python reference functions are narrow fixtures, not completed game logic. The provided Blender/Unreal scaffolds are not validated editor results. Compile and execute the real integration; fix errors with corresponding contract/test updates. Never call a rendered mannequin or basic primitive a finished hero.

Do not invent installed helpers, permissions, builds, screenshots, test counts or performance scores. Missing Blender/Unreal/compiler access blocks verification of those deliverables. Report the specific blocker, finish independent authorized work, and leave an exact resumable handoff; do not label an unverified project playable.

Do not make purchases, upload/publish, register services, enable broad unsafe permissions or download unlicensed assets. Work offline where possible; use current official documentation when checking installed API behavior.

The final handoff must identify the packaged executable path and launch instructions when actually produced; source and data paths; completed assets and outstanding defects; 1H7B and 2H6B evidence; actual all-bot combat test results; frame-time measurements on named hardware; and tests passed, failed or not run. A source-code dump, generated documents or an editor screenshot alone does not satisfy this assignment.
