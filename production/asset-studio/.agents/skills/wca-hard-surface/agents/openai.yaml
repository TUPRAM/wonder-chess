interface:
  display_name: "Hard Surface"
  short_description: "Construct armor, weapons and rigid props with deliberate cross-sections and attachment."
  default_prompt: "Use $wca-hard-surface for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
