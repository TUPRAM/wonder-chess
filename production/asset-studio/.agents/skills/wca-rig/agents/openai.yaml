interface:
  display_name: "Rig"
  short_description: "Build a versioned authoring rig and explicit game export skeleton."
  default_prompt: "Use $wca-rig for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
