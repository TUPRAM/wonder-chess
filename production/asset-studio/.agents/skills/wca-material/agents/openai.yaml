interface:
  display_name: "Material"
  short_description: "Author coherent stylized materials and textures without using them to hide form errors."
  default_prompt: "Use $wca-material for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
