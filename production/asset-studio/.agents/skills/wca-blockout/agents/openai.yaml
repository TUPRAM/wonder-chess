interface:
  display_name: "Blockout"
  short_description: "Build or revise only the large forms and silhouette of an approved asset reference."
  default_prompt: "Use $wca-blockout for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
