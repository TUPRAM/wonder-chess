interface:
  display_name: "Ui"
  short_description: "Produce game icons, portraits and interface art with small-size and state validation."
  default_prompt: "Use $wca-ui for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
