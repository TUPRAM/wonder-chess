interface:
  display_name: "Reference"
  short_description: "Reconcile AI views into a measured construction pack and set up Blender references."
  default_prompt: "Use $wca-reference for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
