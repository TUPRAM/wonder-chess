interface:
  display_name: "Vfx"
  short_description: "Design readable skill effects and timing in Unreal; not new combat mechanics."
  default_prompt: "Use $wca-vfx for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
