interface:
  display_name: "Optimize"
  short_description: "Create and validate distance/platform representations without destroying approved identity."
  default_prompt: "Use $wca-optimize for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
