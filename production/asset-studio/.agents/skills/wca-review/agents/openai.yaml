interface:
  display_name: "Review"
  short_description: "Critique actual asset images or motion against approved references; never self-issue human approval."
  default_prompt: "Use $wca-review for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
