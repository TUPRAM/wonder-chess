interface:
  display_name: "Retopo"
  short_description: "Build or validate motion-ready/runtime topology from approved forms."
  default_prompt: "Use $wca-retopo for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
