interface:
  display_name: "Release"
  short_description: "Prepare final asset promotion after current technical and human acceptance; do not publish automatically."
  default_prompt: "Use $wca-release for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
