interface:
  display_name: "Skin"
  short_description: "Initialize, correct and verify deform weights and actual joint motion."
  default_prompt: "Use $wca-skin for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
