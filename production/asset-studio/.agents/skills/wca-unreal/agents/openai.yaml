interface:
  display_name: "Unreal"
  short_description: "Import and verify approved assets in the actual Unreal project and packaged runtime."
  default_prompt: "Use $wca-unreal for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
