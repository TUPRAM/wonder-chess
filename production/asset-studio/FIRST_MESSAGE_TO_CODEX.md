# First execution — Wonder Chess Asset Studio AS1

Use `$wca-orchestrate` for this task. Read `production/asset-studio/START_HERE.md`, the existing repository instructions, and `production/asset-studio/docs/09_ADA_PILOT.md`.

This is an art-production-system integration into the existing Wonder Chess repository. Do not rebuild the game, change combat balance, rename asset IDs, reset an existing Blender scene, or overwrite the current Ada. Inspect newer local work instead of assuming the public repository is current.

Integrate the `wca-*` skills by a reviewed merge. Run the standalone tests. Probe actual image input, Blender MCP scene reading, image-returning capture, trusted script execution, Blender version, Unreal version and paths. Do not pretend an MCP configuration means those probes passed. Leave telemetry, upload and paid services disabled/unapproved unless authorized.

Create a new Ada candidate workspace at `art-source/asset-studio/wc_u_human_guardian/`. Use the user's illustrated Ada as aesthetic intent and the supplied poor render as the rejected baseline. Reconcile the illustrated sheet's angled views, costume inconsistencies and anatomical left/right before treating anything as modeling geometry. Do not call its front panel a certified orthographic front. Do not generate dozens of independent closeups that alter her face or costume.

First deliver the brief/reference stage: a part inventory; immutable identity rules; crop/turnaround plan; versioned construction decisions; an actual image-reading comparison; and a justified modeling-route decision. Existing geometry is a possible blockout, not a likeness constraint. Prefer an approved suitable base or deliberate mesh/sculpt work if the generic primitive route demonstrably fails.

After Pram approves the reference packet, build an untextured Ada head-and-torso likeness study and one representative shoulder assembly in an isolated source file. Include true front, profile and three-quarter clay renders with fixed cameras, plus honest defects. Do not spend this pass on rigging, LODs, a full costume, export automation or more characters. Continue within the current stage without needless questions, but do not cross the human forms gate by inventing a signoff.

Only after that study is approved should the full body, hands, armor, shield and sword proceed through the documented topology, UV/bake, material, deformation, motion, optimization and Unreal stages. A mature asset must pass a real source revision/reimport and packaged gameplay-camera review. Preserve authoritative gameplay event timing.

Use the actual helper scripts only after inspecting them against installed APIs. The ledger is a local evidence/checkpoint aid, not a trustless approval system. Codex may prepare candidates and technical review records, but may not invoke `approve --role human` on behalf of Pram.

End each bounded run with changed paths, source/review hashes, actual commands and exit codes, captures actually viewed, unresolved defects, current gate and next unblocked operation. Do not report asynchronous work unless a real job was started and its status can be inspected.
