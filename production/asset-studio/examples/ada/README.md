# Ada example inputs

These are the actual supplied illustration and rejected 3D render, copied without enhancement. `crops/` contains native-pixel extracts produced by the included image tool. No new views, model, textures or actual Blender evidence were produced for this example.

The supplied “front” is angled; do not call it a registered orthographic view. The bad render is cut off at the feet. Use the concept as visual intent, resolve its inconsistencies, and obtain human construction approval before modeling.

This is not an accepted asset workspace. Start a separate workspace through assetctl; do not treat the example folder as evidence that any art gate has passed.
