# 03 New View From Geometry

Use {{approved_blockout_projection}} for camera, neutral pose and silhouette constraints and {{selected_identity_image}} for visual identity. Render/design a {{single_view}} construction candidate. Preserve the geometry-derived placement of joints and major parts; add only the approved design refinement {{allowed_refinement}}. Do not independently redesign the face, relocate gear or change the hem. Flag ambiguous hidden construction for a separate decision instead of inventing a contradictory layout. Output is reference art, not a render of the final accepted Blender asset.

## Operator check

Resolve all {{placeholders}} from the active asset record. Feed actual images through the tool’s verified input interface. Record outputs/provider metadata and inspect continuity. No external upload or spending without approval. Do not execute this template as a shell command.
