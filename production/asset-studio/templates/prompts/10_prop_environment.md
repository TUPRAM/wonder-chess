# 10 Prop Environment

Design {{asset_name}} as part of Wonder Chess's {{region_kit}}. Function and size: {{function_dimensions}}. Module interfaces/pivot/grid: {{interfaces}}. Match the approved style/material library {{references}}. Show {{view_or_assembly}} with unambiguous silhouette and repeatable construction. Preserve snapping dimensions and walkability/camera clearance. No unrequested scenery that hides edges, no baked labels, no changed proportions between modules. This is a concept candidate; actual modular compatibility requires a 3D assembly test.

## Operator check

Resolve all {{placeholders}} from the active asset record. Feed actual images through the tool’s verified input interface. Record outputs/provider metadata and inspect continuity. No external upload or spending without approval. Do not execute this template as a shell command.
