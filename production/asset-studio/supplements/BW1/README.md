# Wonder Chess — Body, Costume, and Model-Machine Plan BW1

Prepared 9 September 2026. **Research and execution instructions, not a built body or a trained model.**

## Decision

Keep the demonstrated MPFB anatomical foundation. Start Ada's body/hands/feet from the full indexed source already retained in `MP1_SOURCE_CONTROLS`; do not assemble unrelated online limbs. Build a coherent costume from a small, provenance-reviewed set of compatible examples and original parts. Record successful executions as recipes, then prove reuse on another body before promoting them to automation.

The head remains `ART_REVISE`. This task authorizes independent body/costume research and a temporary deformation study, not head approval or canonical asset promotion. Preserve the 24-hero game and existing AS1/HV1/MR1/ACB1 work.

## Read order

1. `01_FREE_SOURCES.md` — actual candidate packs, item names, licenses and limitations.
2. `02_ADA_BODY_AND_COSTUME_TASK.md` — the next bounded local Blender assignment.
3. `03_RECIPE_LEARNING.md` — turn examples into reproducible construction knowledge.
4. `04_MODEL_MACHINE_ROADMAP.md` — retrieval, fitting, data collection and later learning.
5. `FIRST_MESSAGE_TO_CODEX.md` — starting instruction.

`SOURCE_REGISTRY.json` is the machine-readable source list. `examples/recipe_template.json` and `examples/episode_template.json` are intentionally unexecuted templates. They must not be represented as an implemented tool API or successful training records.

## Placement

Place this folder at `production/body-workflow-bw1/` inside the existing Wonder Chess workspace. Read current repository instructions before modifying anything. Do not replace `AGENTS.md`, the skill library, the source master, or the game. Use a new, collision-safe body-study directory discovered locally.

## Current source facts

The supplied MP1 review says the verified scene contains a 4,271-vertex extracted head, plus a separate full 19,158-vertex MPFB source with 38 shape keys. A preview mask hides the body without deleting its source vertices. MPFB target controls belong on the full indexed source, not the extracted head. Those observations are from the supplied review, not a new Blender inspection performed for this handoff.

The same review explicitly does not establish rigging, deformation, costume, final surfaces, LOD, Unreal or human approval. MPFB's output is a useful foundation, not a finished stylistic match.

## Evidence boundary

Online creator/project pages were researched. No third-party model archives were downloaded, no login or purchase was made, and no model was imported or rendered here. Blender is unavailable in this working environment. Uploaded .blend files are unchanged. No new model training or GPU inference was performed. The only validation here is document/data consistency, source-file preservation and package integrity; see `reports/VALIDATION.md`.

Source links are preserved so Codex can acquire the minimum necessary files locally, inspect the actual licenses and dependencies, and run the real experiments. A source page's claimed features do not substitute for inspection of the downloaded mesh.
