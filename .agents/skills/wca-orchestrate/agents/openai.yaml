interface:
  display_name: "Orchestrate"
  short_description: "Route new or revised Wonder Chess assets through AS1; do not redesign gameplay."
  default_prompt: "Use $wca-orchestrate for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: true
