interface:
  display_name: "Brief"
  short_description: "Define scope, identity, parts, budgets and provenance before creating asset references."
  default_prompt: "Use $wca-brief for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
