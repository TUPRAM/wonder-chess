interface:
  display_name: "Environment"
  short_description: "Produce modular arena/environment/foliage assets with assembly and scene readability checks."
  default_prompt: "Use $wca-environment for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
