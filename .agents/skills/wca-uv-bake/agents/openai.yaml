interface:
  display_name: "Uv Bake"
  short_description: "Unwrap, plan texture density and bake approved source detail onto runtime geometry."
  default_prompt: "Use $wca-uv-bake for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
