interface:
  display_name: "Audio"
  short_description: "Create or integrate game sound assets with provenance, listening and in-context mix tests."
  default_prompt: "Use $wca-audio for the current Wonder Chess asset and its active gate."
policy:
  allow_implicit_invocation: false
