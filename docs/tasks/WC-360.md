# WC-360 — Packaged alpha verification and handoff

**Status on delivery of this kit:** NOT EXECUTED. This is a local Codex task, not completed game work.

## Prerequisites

WC-300 through WC-350 pass with explicitly recorded exceptions; all twelve art gates pass.

## Read these inputs

- `docs/QA_ACCEPTANCE.md`
- `docs/MASTER_IMPLEMENTATION_v3.md`
- `tools/unreal/package_game.ps1`
- `reports/implementation_state.json`

## Ownership

Release integrator owns package/manifest; independent QA owns acceptance report. Do not change balance opportunistically during release validation.

## Implementation

1. Build/cook/package the actual Windows Development alpha; launch outside the source tree without editors, web server or AI calls.

2. Execute at least 100 seeded 0H8B tournaments through the actual game combat runner. Report actual seeds, hashes, trial counts, outcomes and all failures; 700 Python pairing fixtures do not count.

3. Complete manual 1H7B, early human elimination, spectating, settings/resolution/language changes, full results and repeated restart.

4. Measure CPU/GPU frame times and memory for busy twelve-unit fights on named hardware at recorded presets. Target 1080p60 remains a target until measured; no mobile claim.

5. Deliver source, actual packaged executable path, controls, tested configuration, accepted asset list, evidence index, known issues and resumable next milestone. Keep speculative 24-hero expansion separate.

## Required outputs

- builds/Win64/ actual package if successful
- reports/WC-360/100-engine-matches.json
- reports/WC-360/performance-and-manual-review.md
- reports/RELEASE_HANDOFF.md

## Acceptance

- Standalone game meets the complete promised feature/art contract and can be played without developer intervention.
- Actual editor/game execution evidence is present and traceable to the delivered content/build.
- No fake green status for missing network, art, package or combat tests.

## Failure, recovery and evidence

No executable, incomplete roster presentation, missing full tournament, major rule defects or fabricated evidence prevents the playable label. Report partial work honestly and preserve the next exact task.

Before changes, checkpoint owned files without overwriting existing user work. Record actual commands, exit codes, tested engine/data version, source commit/hash, captured evidence and remaining issues under this task ID. Never infer screenshots or success from code inspection. Roll back only this task’s changes through reviewed version control. Update `reports/implementation_state.json` with real evidence. When one lane is blocked, continue independent permitted lanes; do not silently replace its acceptance criteria.
