# WC-300 — Toolchain and first package

**Status on delivery of this kit:** NOT EXECUTED. This is a local Codex task, not completed game work.

## Prerequisites

No prerequisite; new isolated workspace.

## Read these inputs

- `AGENTS.md`
- `START_HERE.md`
- `docs/MASTER_IMPLEMENTATION_v3.md`
- `tools/preflight.py`

## Ownership

Project integrator owns game project/bootstrap/config and environment reports. No character artist changes the runtime module yet.

## Implementation

1. Inspect the current directory and existing Wonder Chess progress. Never migrate or alter VEILMARK. Record installed Blender, Unreal, build tools and SDK without inventing access.

2. Run the kit baseline checks, preserve their initial evidence, and review source/data version. Create a new C++ Unreal project using the installed supported route; choose a compatible engine version rather than an unchecked newest version.

3. Create an explicit minimal menu and arena test map, set default map/game mode, compile, cook and package a Development Windows executable. Launch from the archive directory.

4. Record errors precisely. A missing editor/toolchain blocks game verification, not independent data review. Continue permitted next tasks when possible; a checkpoint does not end the whole assignment.

## Required outputs

- game/WonderChess.uproject and initial runtime module/config/maps
- reports/WC-300/environment.json
- reports/WC-300/build-and-package.log
- reports/WC-300/launch-evidence.md

## Acceptance

- All supplied schema/reference checks pass or reviewed deviations are documented.
- The engine project compiles and a real packaged executable reaches its actual menu.
- No claim that an empty map is the final alpha; source, engine and archive paths are recorded.

## Failure, recovery and evidence

A build failure, unavailable engine/compiler or unsafe target directory blocks this gate. Preserve logs and report required action without switching to a browser game.

Before changes, checkpoint owned files without overwriting existing user work. Record actual commands, exit codes, tested engine/data version, source commit/hash, captured evidence and remaining issues under this task ID. Never infer screenshots or success from code inspection. Roll back only this task’s changes through reviewed version control. Update `reports/implementation_state.json` with real evidence. When one lane is blocked, continue independent permitted lanes; do not silently replace its acceptance criteria.
