# WC-330 — Twelve coherent playable heroes

**Status on delivery of this kit:** NOT EXECUTED. This is a local Codex task, not completed game work.

## Prerequisites

WC-320 reference gate and stable runtime/presentation contract from WC-310.

## Read these inputs

- `docs/ALPHA_12_ASSET_BRIEFS.md`
- `docs/heroes/`
- `data/units.json`
- `docs/BLENDER_PRODUCTION.md`

## Ownership

One named owner per .blend/.uasset. Divide hero folders by artist and import sequentially; no parallel edits to family rig or master material.

## Implementation

1. Produce the remaining eleven alpha heroes with their unique dossiers; do not model expansion heroes yet. Validate standard/slender/stocky/broad rig families through actual poses.

2. Provide meshes, textures, rig, seven clips, sockets and portraits for each. Shared motion is acceptable after hero-specific deformation/timing review.

3. Integrate presentation definitions with stable IDs. Each active skill uses the same runtime primitive as its authored data; do not add hidden dash damage, stun damage or animation-driven extra hits.

4. Create per-skill readable effects/audio and inspect all 84 hero/clip combinations. Keep colors, team indicators, silhouettes and size consistent.

5. Validate all heroes through a deterministic acquisition/review scene plus real shop rules; a rare offer does not substitute for an acquisition test.

## Required outputs

- 12 accepted source/export/presentation packages
- 12 model-matched portraits
- reports/WC-330/hero-readiness-matrix.json
- reports/WC-330/84-clip-review.md

## Acceptance

- Exactly the twelve alpha IDs are enabled, each with actual accepted art and skill behavior.
- No missing meshes, T-poses, unbound clips, cosmetic-to-rules mismatch or expansion-only asset gate.
- Every hero passes crowded-board readability and material/rig budget review.

## Failure, recovery and evidence

One generic recolored character does not satisfy twelve silhouettes. Missing major animation or skill behavior is a release blocker; record nonblocking minor art issues.

Before changes, checkpoint owned files without overwriting existing user work. Record actual commands, exit codes, tested engine/data version, source commit/hash, captured evidence and remaining issues under this task ID. Never infer screenshots or success from code inspection. Roll back only this task’s changes through reviewed version control. Update `reports/implementation_state.json` with real evidence. When one lane is blocked, continue independent permitted lanes; do not silently replace its acceptance criteria.
