# WC-350 — Two humans plus six bots

**Status on delivery of this kit:** NOT EXECUTED. This is a local Codex task, not completed game work.

## Prerequisites

WC-310 authority and WC-340 UI available; complete actual multi-instance test.

## Read these inputs

- `docs/UNREAL_IMPLEMENTATION.md`
- `docs/TOURNAMENT_AND_BOTS.md`
- `docs/QA_ACCEPTANCE.md`

## Ownership

Networking engineer/integrator owns RPCs, replicated state and session configuration; no balance edits during synchronization diagnosis.

## Implementation

1. Launch two real local/LAN game instances with one authority, two human seats and six bots. Do not relabel two bots as humans.

2. Test owner-authenticated purchases, repeated requests, stale phase/revision, malformed placement, out-of-order sequences and invalid seat access.

3. Verify private shop/bench/gold access at the replicated-data level, not just hidden UI. Observe running remote encounters and reconstruct visual state.

4. Disconnect non-host: preserve its state and replace its command source with a legal bot. Host disconnect: explicit abort without a false winner. Rejoin/host migration are out of alpha scope.

5. Collect actual per-instance logs and videos/screenshots. Later 8H online hosting remains unverified until separately tested.

## Required outputs

- 2H6B session configuration
- reports/WC-350/server.log and client.log
- reports/WC-350/authority-privacy-disconnect-tests.md

## Acceptance

- Both humans can complete the match with synchronized authoritative outcomes.
- Private data is actually restricted and duplicated inputs cannot duplicate resources.
- Disconnect policies match the written contract.

## Failure, recovery and evidence

A single-process eight-bot test is not a network test. Missing real second instance or untested privacy keeps this gate open.

Before changes, checkpoint owned files without overwriting existing user work. Record actual commands, exit codes, tested engine/data version, source commit/hash, captured evidence and remaining issues under this task ID. Never infer screenshots or success from code inspection. Roll back only this task’s changes through reviewed version control. Update `reports/implementation_state.json` with real evidence. When one lane is blocked, continue independent permitted lanes; do not silently replace its acceptance criteria.
